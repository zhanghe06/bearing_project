# Vim

vim一共有4个模式：
- 正常模式 (Normal-mode) Esc
- 插入模式 (Insert-mode) i
- 命令模式 (Command-mode) :
- 可视模式 (Visual-mode) v

正常模式

```
Esc
:set nu     # 显示行号

Esc
[           # 当前页的头部
]           # 当前页的底部
^           # 当前行的头部
$           # 当前行的尾部
```
